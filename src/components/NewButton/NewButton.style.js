import styled from 'styled-components';

export const Button = styled.button`
  background: ${(props) => props.color};
  /* border-radius: 10px; */
  color: white;
  padding: 10px;
  cursor: pointer;
  padding: 1rem 2rem;
  border: none;
`;
